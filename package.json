{
  "name": "sqdwz-information-hub",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:policy": "node ./scripts/sync-policy-data.mjs",
    "build": "node ./scripts/build.mjs",
    "deploy": "npm run build && npx --yes wrangler@latest deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
